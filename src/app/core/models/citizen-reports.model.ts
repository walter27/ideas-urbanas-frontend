import { UtilsService } from '../services/utils.service';
export interface CitizenReports {
    _id: string;
    name: string;
    description: string;
    UtilsService: string;
    image_route: string;
}
